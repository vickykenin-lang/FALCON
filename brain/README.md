# Brain

Reasoning, planning, orchestration and decision engine.

This module must not directly own memory storage, UI state, transport, scheduler state, or external tool implementations. Those capabilities are accessed through versioned contracts/adapters.
