# Interface

Live human/FALCON interaction and observability surface.

The interface consumes runtime events and exposes commands/status without owning Brain, Memory or Execution internals. It can therefore be redesigned independently.
